{
  "name": "jlpt-study",
  "version": "1.0.0",
  "description": "JLPT N5–N3 study app — kanji, kana, grammar, Pokémon quiz",
  "scripts": {
    "dev": "npx serve public -p 3000",
    "build": "echo 'Static site — no build step needed'",
    "start": "npx serve public -p 3000"
  },
  "devDependencies": {
    "serve": "^14.0.0"
  }
}
